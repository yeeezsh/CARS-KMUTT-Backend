scalar Date
scalar JSON

type User {
  username: String
  password: String
  email: String
  permission: String
}

type CreateUserInput {
  username: String
  password: String
  email: String
  permission: String
}

type Query  {
    Users: [User]
    User(id: ID!, permission: String) : User
    Staff(id: ID) : User
    Requestor(id: ID) : User
}